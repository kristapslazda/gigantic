<div class="container" style="padding-top: 60px;padding-bottom: 30px;">
  <div class="row">
    <div class="col-md medium-up--five-sixths medium-up--push-one-twelfth">
      <div class="section-header text-center">
        {% unless page.handle == "nyhedsbrev" %}
        <h1>{{ page.title }}</h1>
        {% endunless %}
      </div>
      <div class="rte">
        {{ page.content }}
      </div>
      {% section 'product-slider-3' %}
    </div>
  </div>
</div>